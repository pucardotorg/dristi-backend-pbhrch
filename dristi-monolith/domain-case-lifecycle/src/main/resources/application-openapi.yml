# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for openapi (subdomain=openapi).
# Lives in domain-case-lifecycle/src/main/resources/ so its config travels with the
# domain-case-lifecycle module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,openapi,...

app:
  timezone: Asia/Kolkata
  zone:
    id: Asia/Kolkata
otel:
  traces:
    exporter: otlp
  logs:
    exporter: none
  metrics:
    exporter: none
  exporter:
    otlp:
      endpoint: http://jaeger-collector.tracing:4318
      protocol: http/protobuf
  instrumentation:
    kafka:
      enabled: true
      experimental-span-attributes: true
    http:
      server:
        ignore-urls: /OpenAPI/health,/OpenAPI/prometheus
kafka:
  consumer:
    config:
      auto_commit: true
      auto_commit_interval: 100
      session_timeout: 15000
      auto_offset_reset: earliest
  producer:
    config:
      retries_config: 0
      batch_size_config: 16384
      linger_ms_config: 1
      buffer_memory_config: 33554432
  topics:
    receipt:
      create: dss-collection
    consumer: service-consumer-topic
egov:
  localization:
    host: https://dev.digit.org
    workDir:
      path: /localization/messages/v1
    context:
      path: /localization/messages/v1
    search:
      endpoint: /_search
    statelevel: true
  mdms:
    host: https://dev.digit.org
    search:
      endpoint: /egov-mdms-service/v1/_search
  hrms:
    host: https://dev.digit.org
    search:
      endpoint: /egov-hrms/employees/_search
  user:
    host: http://localhost:9000
    context:
      path: /user
    create:
      path: /users/_createnovalidate
    search:
      path: /v1/_search
    update:
      path: /users/_updatenovalidate
  idgen:
    host: https://dev.digit.org/
    path: egov-idgen/id/_generate
  workflow:
    host: https://dev.digit.org
    transition:
      path: /egov-workflow-v2/egov-wf/process/_transition
    businessservice:
      search:
        path: /egov-workflow-v2/egov-wf/businessservice/_search
    processinstance:
      search:
        path: /egov-workflow-v2/egov-wf/process/_search
  url:
    shortner:
      host: https://dev.digit.org
      endpoint: /egov-url-shortening/shortener
  sms:
    notification:
      topic: egov.core.notification.sms
  inbox:
    host: http://localhost:8059
    search:
      endpoint: /inbox/v2/index/_search
    getfield:
      search:
        endpoint: /inbox/v2/_getFields
  advocate:
    host: http://localhost:8080
    path: /advocate/v1/_search
  filestore:
    host: http://localhost:8081
  file:
    store:
      get:
        endpoint: /filestore/v1/files/id
      save:
        endpoint: /filestore/v1/files
  order:
    host: http://localhost:8080
    search:
      path: /order/v1/search
  case:
    host: http://localhost:8080/
    search:
      path: case/v1/_search
  billing:
    host: http://localhost:8090
    fetchbill:
      endpoint: /billing-service/bill/v2/_fetchbill
    searchbill:
      endpoint: /billing-service/bill/v2/_search
  etreasury:
    host: http://localhost:8876
    get:
      breakdown:
        endpoint: /etreasury/payment/v1/_getHeadBreakDown
      payment:
        receipt:
          endpoint: /etreasury/payment/v1/_getPaymentReceipt
    process:
      challan:
        endpoint: /etreasury/payment/v1/_processChallan
  task:
    management:
      host: http://localhost:8052
      create:
        endpoint: /task-management/v1/_create
      update:
        endpoint: /task-management/v1/_update
      search:
        endpoint: /task-management/v1/_search
  payment:
    calculator:
      host: http://localhost:8053
      calculate:
        endpoint: /payment-calculator/v1/_calculate
  lock:
    host: http://localhost:8054
    get:
      endpoint: /lock-svc/v1/_get
    release:
      endpoint: /lock-svc/v1/_release
    set:
      endpoint: /lock-svc/v1/_set
  infra:
    indexer:
      host: http://localhost:9200/
  indexer:
    es:
      username: elastic
      password: 8fwbD6HbJh6HU0oddsHm8TEI
  individual:
    host: http://localhost:8080/
    search:
      path: individual/v1/_search
is:
  workflow:
    enabled: true
  elasticsearch:
    enabled: false
case:
  service:
    host: http://localhost:9090
    searchbycnrnumber:
      endpoint: /case/v1/search/cnrNumber
    searchbycasetype:
      endpoint: /case/v1/search/caseType
    searchbycasenumber:
      endpoint: /case/v1/search/caseNumber
    searchbycasesearchtext:
      endpoint: /case/v1/search/caseSearchText
    search:
      endpoint: /case/v1/_search
    add:
      address:
        endpoint: /case/v1/address/_add
  status:
    disposed:
      outcomes: DISMISSED,ALLOWED,PARTIALLYALLOWED,CONVICTED,PARTIALLYCONVICTED,ABATED,WITHDRAWN,SETTLED,TRANSFERRED
    allowed:
      statuses: UNDER_SCRUTINY,PENDING_REGISTRATION,CASE_REASSIGNED,PENDING_RE_E-SIGN,PENDING_RE_SIGN,PENDING_NOTICE,PENDING_RESPONSE,PENDING_ADMISSION,CASE_ADMITTED,CASE_DISMISSED,RE_PENDING_PAYMENT,PENDING_ADMISSION_HEARING
hearing:
  service:
    host: http://localhost:8081
    search:
      endpoint: /hearing/v1/search
judge:
  name: Smt. Soorya S Sukumaran
module:
  names:
    enabled: landing-page,DRISTI,treasury,mediation-document
bail:
  service:
    host: http://localhost:8081
    search:
      endpoint: /bail-bond/v1/_search
    update:
      endpoint: /bail-bond/v1/_update
esign:
  service:
    host: http://localhost:8089/e-sign-svc
    endpoint: /v1/_esign
egov-state-level-tenant-id: pb
evidence:
  service:
    host: http://localhost:9093
    search:
      endpoint: /evidence/v1/_search
    update:
      endpoint: /evidence/v1/_update
digitalize:
  service:
    host: http://localhost:8080
    search:
      endpoint: /digitalized-documents/v1/_search
    update:
      endpoint: /digitalized-documents/v1/_update
elastic:
  pending:
    task:
      endpoint: /pending-tasks-index
      search: /_search
dristi:
  analytics:
    host: http://localhost:8060
    offline:
      payment:
        endpoint: /analytics/offline-payment/_create
logging:
  loki:
    enabled: false
    url: http://localhost:3100/loki/api/v1/push
  level:
    root: INFO
