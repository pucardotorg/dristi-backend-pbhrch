# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for template-configuration (subdomain=templateconfiguration).
# Lives in domain-case-lifecycle/src/main/resources/ so its config travels with the
# domain-case-lifecycle module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,templateconfiguration,...

app:
  timezone: Asia/Kolkata
otel:
  instrumentation:
    http:
      server:
        ignore-urls: /template-configuration/health,/template-configuration/prometheus
kafka:
  consumer:
    config:
      auto_commit: true
      auto_commit_interval: 100
      session_timeout: 15000
      auto_offset_reset: earliest
  producer:
    config:
      retries_config: 0
      batch_size_config: 16384
      linger_ms_config: 1
      buffer_memory_config: 33554432
egov:
  kafka:
    template:
      save:
        topic: save-template-configuration
      update:
        topic: update-template-configuration
logging:
  loki:
    enabled: false
    url: http://localhost:3100/loki/api/v1/push
  level:
    root: INFO
