# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for payment-calculator-svc (subdomain=calculator).
# Lives in domain-payments/src/main/resources/ so its config travels with the
# domain-payments module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,calculator,...

app:
  timezone: Asia/Kolkata
otel:
  traces:
    exporter: otlp
  logs:
    exporter: none
  metrics:
    exporter: none
  exporter:
    otlp:
      endpoint: http://jaeger-collector.tracing:4318
      protocol: http/protobuf
  instrumentation:
    kafka:
      enabled: true
      experimental-span-attributes: true
    http:
      server:
        ignore-urls: /payment-calculator/health,/payment-calculator/prometheus
kafka:
  consumer:
    config:
      auto_commit: true
      auto_commit_interval: 100
      session_timeout: 15000
      auto_offset_reset: earliest
  producer:
    config:
      retries_config: 0
      batch_size_config: 16384
      linger_ms_config: 1
      buffer_memory_config: 33554432
  topics:
    receipt:
      create: dss-collection
egov:
  mdms:
    host: http://localhost:8020
    search:
      endpoint: /egov-mdms-service/v1/_search
  sms:
    notification:
      topic: egov.core.notification.sms
drishti:
  postalhub:
    create:
      topic: postal-hub-create
    update:
      topic: postal-hub-update
  summon:
    pages:
      count: 2
logging:
  loki:
    enabled: false
    url: http://localhost:3100/loki/api/v1/push
  level:
    root: INFO
