# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for esign-interceptor (subdomain=esign).
# Lives in dristi-app/src/main/resources/ (no target_module) so its config travels with the
# domain-<none> module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,esign,...

app:
  timezone: Asia/Kolkata
otel:
  traces:
    exporter: otlp
  logs:
    exporter: none
  metrics:
    exporter: none
  exporter:
    otlp:
      endpoint: http://jaeger-collector.tracing:4318
      protocol: http/protobuf
  instrumentation:
    kafka:
      enabled: true
      experimental-span-attributes: true
    http:
      server:
        ignore-urls: /esign-interceptor/health,/esign-interceptor/prometheus
egov:
  filestore:
    host: http://localhost:8052/
    search:
      endpoint: filestore/v1/files/id
drishti:
  esign:
    host: http://localhost:8051
    endpoint: /e-sign-svc/v1/_signed
    redirect:
      url: https://dristi-kerala-dev.pucar.org
    landing:
      page:
        redirect:
          url: /landing
  oath:
    host: https://dristi-kerala-dev.pucar.org/
    endpoint: user/oauth/token?_=1713357247536
dristhi:
  oath:
    username: esigninterceptor
    password: Beehyv@123
    tenantId: pb
    usertype: EMPLOYEE
    scope: read
    grantType: password
logging:
  loki:
    enabled: false
    url: http://localhost:3100/loki/api/v1/push
  level:
    root: INFO
