/**
 * Shared Spring config: CommonConfiguration (@Value-bound host/path properties
 * used by the canonical *Util classes) and CommonConstants (string constants
 * referenced across the canonical implementations).
 */
@org.springframework.modulith.NamedInterface("config")
package org.pucar.dristi.common.config;
