/**
 * Shared Kafka producer (Producer wraps KafkaProducerService for structured
 * success/failure logging on every push).
 */
@org.springframework.modulith.NamedInterface("kafka")
package org.pucar.dristi.common.kafka;
