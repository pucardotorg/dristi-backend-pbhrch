# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for advocate-office-management (subdomain=advocateoffice).
# Lives in domain-identity-access/src/main/resources/ so its config travels with the
# domain-identity-access module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,advocateoffice,...

app:
  timezone: Asia/Kolkata
otel:
  traces:
    exporter: otlp
  logs:
    exporter: none
  metrics:
    exporter: none
  exporter:
    otlp:
      endpoint: http://jaeger-collector.tracing:4318
      protocol: http/protobuf
  instrumentation:
    kafka:
      enabled: true
      experimental-span-attributes: true
    http:
      server:
        ignore-urls: /advocate-office-management/health,/advocate-office-management/prometheus
kafka:
  consumer:
    config:
      auto_commit: true
      auto_commit_interval: 100
      session_timeout: 15000
      auto_offset_reset: earliest
  producer:
    config:
      retries_config: 0
      batch_size_config: 16384
      linger_ms_config: 1
      buffer_memory_config: 33554432
  topics:
    receipt:
      create: dss-collection
    consumer: service-consumer-topic
egov:
  localization:
    host: https://dev.digit.org
    workDir:
      path: /localization/messages/v1
    context:
      path: /localization/messages/v1
    search:
      endpoint: /_search
    statelevel: true
  mdms:
    host: http://localhost:8084
    search:
      endpoint: /egov-mdms-service/v1/_search
  hrms:
    host: https://dev.digit.org
    search:
      endpoint: /egov-hrms/employees/_search
  user:
    host: https://dev.digit.org
    context:
      path: /user/users
    create:
      path: /_createnovalidate
    search:
      path: /user/_search
    update:
      path: /_updatenovalidate
  idgen:
    host: https://dev.digit.org/
    path: egov-idgen/id/_generate
  workflow:
    host: https://dev.digit.org
    transition:
      path: /egov-workflow-v2/egov-wf/process/_transition
    businessservice:
      search:
        path: /egov-workflow-v2/egov-wf/businessservice/_search
    processinstance:
      search:
        path: /egov-workflow-v2/egov-wf/process/_search
  url:
    shortner:
      host: https://dev.digit.org
      endpoint: /egov-url-shortening/shortener
  sms:
    notification:
      topic: egov.core.notification.sms
is:
  workflow:
    enabled: true
dristi:
  individual:
    host: http://localhost:8081
    search:
      endpoint: /individual/v1/_search
  case:
    host: http://localhost:9091
    member:
      search:
        endpoint: /case/v1/_searchCaseMember
  process:
    case:
      member:
        endpoint: /case/v1/_processCaseMember
add:
  member:
    kafka:
      create:
        topic: add-advocate-office-member
leave:
  office:
    kafka:
      create:
        topic: leave-advocate-office
update:
  member:
    access:
      kafka:
        topic: update-advocate-office-member-access
spring:
  redis:
    host: localhost
    port: 6379
    timeout: 10
