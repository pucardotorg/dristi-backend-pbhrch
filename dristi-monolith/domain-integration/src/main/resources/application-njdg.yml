# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for njdg-transformer (subdomain=njdg).
# Lives in domain-integration/src/main/resources/ so its config travels with the
# domain-integration module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,njdg,...

app:
  timezone: Asia/Kolkata
  zone:
    id: Asia/Kolkata
  allowed:
    tenant:
      ids: pb
otel:
  instrumentation:
    http:
      server:
        ignore-urls: /njdg-transformer/health,/njdg-transformer/prometheus
kafka:
  consumer:
    config:
      auto_commit: true
      auto_commit_interval: 100
      session_timeout: 15000
      auto_offset_reset: earliest
  producer:
    config:
      retries_config: 0
      batch_size_config: 16384
      linger_ms_config: 1
      buffer_memory_config: 33554432
  topic:
    case:
      outcome: case-outcome-topic
      overall:
        status: case-overall-status-topic
      conversion: update-case-conversion
    update:
      advocate:
        application: update-advocate-application
        details: update-advocate-details
      hearing:
        details: update-hearing-details
    save:
      case:
        details: save-case-details
        conversion:
          details: save-case-conversion-details
      order:
        details: save-order-details
      hearing:
        details: save-hearing-details
      extra:
        parties: save-extra-parties
        advocate:
          details: save-extra-advocate-details
      advocate:
        details: save-advocate-details
      act:
        details: save-act-details
  topics:
    join:
      case: litigant-join-case,representative-join-case,join-case-update-case-additional-details-v2,join-case-update-pending-advocate-requests,poa-join-case
    case: update-case-application,update-case-status-application,lpr-case-details-update
    order: update-order-application
  notification:
    order:
      topic: update-notification
egov:
  mdms:
    host: http://localhost:8020
    search:
      endpoint: /egov-mdms-service/v1/_search
  filestore:
    host: http://localhost:8070
    path: /filestore/v1/files/id
  hrms:
    host: http://localhost:8081
    search:
      endpoint: /egov-hrms/employees/_search
  hearing:
    host: http://localhost:8092/
    path: hearing/v1/update_transcript_additional_attendees
    search:
      path: hearing/v1/search
  case:
    host: http://localhost:9090/
    search:
      path: /case/v1/_search
  individual:
    host: http://localhost:8081/
    search:
      path: individual/v1/_search
  inbox:
    host: http://localhost:8059
    search:
      endpoint: /inbox/v2/index/_search
      limit: 300
      offset: 0
dristi:
  order:
    host: http://localhost:8091
    search:
      endpoint: /order/v1/search
judge:
  designation: Judicial Magistrate 1st Class
  code: KL01002
court:
  number: 1
state:
  code: 32
cicri:
  type: 3
judgement:
  order:
    type: 1
    document:
      type: 16
notification:
  order:
    business:
      template: No sitting notified on {hearingDate}. Case posted to {nextDate}.
    processing:
      delay:
        ms: 5000
