# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for icops_integration-kerala (subdomain=icops).
# Lives in domain-integration/src/main/resources/ so its config travels with the
# domain-integration module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,icops,...

server:
  context-path: /kerala-icops
  tomcat:
    max-http-post-size: 52428800
otel:
  traces:
    exporter: otlp
  logs:
    exporter: none
  metrics:
    exporter: none
  exporter:
    otlp:
      endpoint: http://jaeger-collector.tracing:4318
      protocol: http/protobuf
  instrumentation:
    kafka:
      enabled: true
      experimental-span-attributes: true
    http:
      server:
        ignore-urls: /kerala-icops/health,/kerala-icops/prometheus
kafka:
  consumer:
    config:
      auto_commit: true
      auto_commit_interval: 100
      session_timeout: 15000
      auto_offset_reset: earliest
  producer:
    config:
      retries_config: 0
      batch_size_config: 16384
      linger_ms_config: 1
      buffer_memory_config: 33554432
client:
  id: dristi-app
  secret: b1f91ebe-d6ef-46ce-87e4-3abf0bf432c2
grant:
  type: client_credentials
icops:
  url: https://staging-icops.keralapolice.gov.in/icops/api
auth:
  endpoint: /getAuthToken
  type: get_auth_token
process:
  request:
    endpoint: /eCourt/sendPR
location:
  jurisdiction:
    endpoint: /getLocationBasedJurisdiction
egov-state-level-tenant-id: pb
egov:
  file:
    store:
      host: http://localhost:8087
      search:
        endpoint: /filestore/v1/files/id
      save:
        endpoint: /filestore/v1/files
      summons:
        module: summons
  mdms:
    host: https://dev.digit.org
    search:
      endpoint: /egov-mdms-service/v1/_search
    icops:
      business:
        service:
          name: icops
  summons:
    host: http://egov-summons:8080
    update:
      endpoint: /summons-svc/summons/v1/_updateSummons
  process:
    origin: DRISTI
    inv:
      agency: Police
  idgen:
    host: http://egov-idgen:8080
    path: /egov-idgen/id/_generate
    name: icops.sh
  oauth:
    url: https://dristi-kerala-dev.pucar.org/user/oauth/token?_=1713357247536
  user:
    username: police-update
    password: Dristi@123
service:
  name: icops
  ky: service_key
app:
  zone:
    id: Asia/Kolkata
logging:
  loki:
    enabled: false
    url: http://localhost:3100/loki/api/v1/push
  level:
    root: INFO
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
