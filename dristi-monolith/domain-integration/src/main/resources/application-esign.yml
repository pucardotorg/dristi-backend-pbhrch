# AUTO-GENERATED by scripts/migration/config_consolidation/run_consolidation.py
# Per-subdomain overlay for e-sign-svc (subdomain=esign).
# Lives in domain-integration/src/main/resources/ so its config travels with the
# domain-integration module.
# Includes service-unique keys + this service's value for any key that conflicts across services.
# Activate via spring.profiles.active=...,esign,...

spring:
  datasource:
    enabled: false
  http:
    multipart:
      enabled: true
    converters:
      multipart:
        enabled: true
        serializer:
          class: org.springframework.http.converter.ByteArrayHttpMessageConverter
app:
  timezone: Asia/Kolkata
otel:
  traces:
    exporter: otlp
  logs:
    exporter: none
  metrics:
    exporter: none
  exporter:
    otlp:
      endpoint: http://jaeger-collector.tracing:4318
      protocol: http/protobuf
  instrumentation:
    kafka:
      enabled: true
      experimental-span-attributes: true
    http:
      server:
        ignore-urls: /e-sign-svc/health,/e-sign-svc/prometheus
kafka:
  consumer:
    config:
      auto_commit: true
      auto_commit_interval: 100
      session_timeout: 15000
      auto_offset_reset: earliest
  producer:
    config:
      retries_config: 0
      batch_size_config: 16384
      linger_ms_config: 1
      buffer_memory_config: 33554432
  topics:
    receipt:
      create: dss-collection
egov:
  mdms:
    host: https://dev.digit.org
    search:
      endpoint: /egov-mdms-service/v1/_search
  sms:
    notification:
      topic: egov.core.notification.sms
  filestore:
    host: http://localhost:8052/
    search:
      endpoint: filestore/v1/files/id
    create:
      endpoint: filestore/v1/files
    delete:
      endpoint: /filestore/v1/files/delete
esing:
  ver: '2.1'
  sc: Y
  asp:
    id: ESGN-900
  auth:
    mode: 1
  response:
    sig:
      type: pkcs7
    url: https://dristi-kerala-dev.pucar.org/esign-interceptor/v1/_intercept
  id: 1
  hash:
    algorithm: SHA256
  doc:
    info: My Document
  ekyc:
    id:
      type: A
esign:
  create:
    topic: create-esign
  update:
    topic: update-esign
  position:
    offset: 2
  y:
    coordinate:
      offset: 20
# HAND-CURATED — interceptor-only keys carried over from esign-interceptor
# during the e-sign-svc + esign-interceptor two-source merge (registry row,
# PR #84). Consolidation strips these on each regen because esign-interceptor
# has no target_module in its manifest; re-add this block whenever a future
# regen drops it.
drishti:
  esign:
    redirect:
      url: https://dristi-kerala-dev.pucar.org
    landing:
      page:
        redirect:
          url: /landing
logging:
  loki:
    enabled: false
    url: http://localhost:3100/loki/api/v1/push
  level:
    root: INFO
